# Ejemplo de variables de entorno para ihs_status_to_appd.py
# Copiar a env.sh, editar valores y (opcional) source env.sh antes de arrancar el Machine Agent,
# o definir estas variables en el script/systemd que inicia el Machine Agent.

export IHS_STATUS_URL="http://10.10.10.20:80/server-status?auto"
export APPD_HTTP_LISTENER="http://127.0.0.1:8293/api/v1/metrics"
export METRIC_PREFIX="Custom Metrics|Web|IHS|HPUX"
